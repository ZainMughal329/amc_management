

import 'package:flutter/material.dart';

class allFilesView extends StatelessWidget {
  const allFilesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
